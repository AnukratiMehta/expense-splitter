<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <h1><%= title %></h1>

    <% if (currentUser) { %>
      <p>Logged in as <strong><%= currentUser.username %></strong></p>
    <% } else { %>
      <p>You are not logged in yet.</p>
    <% } %>

    <p>This is the starting point of your secure Expense Splitter app.</p>
  </body>
</html>
